# Airnobe：产品与阅读行为契约

## 产品定位

Airnobe 是面向中文用户的日本轻小说阅读器，不是通用 EPUB 排版器。EPUB 只作为输入，阅读器消费统一的类型化 AST。

当前正式支持 Windows 桌面端。Android、移动网页和触摸交互不在当前范围内。

## 内容与注音

一个文本块可以同时包含中文译文和日文原文。没有可靠结构证据时不猜测配对，纯中文、纯日文和未分类内容分别保留。

P0 保留出版社 ruby，P0.5 可选地生成辅助振假名和片假名罗马音：

- `origin: "source"`：出版社注音，属于日文原文。
- `origin: "reused"`：复用书内唯一的出版社读音。
- `origin: "generated"`：由词典和分词器生成的读音。
- `readingType: "kana"`：出版社或程序振假名。
- `readingType: "romaji"`：仅对片假名词生成的改良赫本式罗马音，长音使用长音符。
- 低置信度、人名、造语和无法安全对齐的词保持原文。

出版社、书内复用和词典生成振假名分别使用金、紫、青三种颜色。`E` 同时控制后两类辅助振假名，不影响出版社注音；`Z` 独立控制片假名罗马音。两项可以同时开启，日文隐藏时仍可改变其状态。

阅读器支持三种结果：

| 状态       | 中文 | 日文 | 出版社注音 | 程序振假名 |
| ---------- | ---- | ---- | ---------- | ---------- |
| 纯中文     | 显示 | 隐藏 | 隐藏       | 隐藏       |
| 中日原始版 | 显示 | 显示 | 显示       | 隐藏       |
| 中日注音版 | 显示 | 显示 | 显示       | 显示       |

片假名罗马音只表示日语读音，例如 `コンピューター → konpyūtā`，不尝试恢复 `computer` 等原始外语拼写。

## 桌面阅读行为

- 默认 Airnobe 夜间主题、单列连续滚动。新书的日文、辅助注音和片假名罗马音默认关闭。
- 长书使用虚拟列表，只挂载视口附近的块。
- 正文排版区分段内间距、普通段落间距和 EPUB 明确空段形成的大段分隔。大段落总间距为一个当前正文行高加两倍段落间距；空段不成为导航或进度锚点。
- 切换显示层时，以当前文本块及其视口偏移为阅读锚点。
- 日文与中文左边缘对齐，使用栏外引用线区分；默认透明度为 `0.6`。
- 右键或默认 `1` 键打开居中的阅读菜单；常驻界面不显示工具栏。
- 中文、日文和 UI 分别使用 Sarasa Gothic SC、Sarasa Gothic J 和 Sarasa UI SC；正文提供 400/600 两档真实字重。

默认快捷键：

| 按键          | 行为                                             |
| ------------- | ------------------------------------------------ |
| `Q`         | 显示或隐藏日文                                   |
| `E`         | 显示或隐藏书内复用与词典生成注音                 |
| `Z`         | 显示或隐藏片假名罗马音                           |
| `W`         | 以视口底部单位为基准，按回退/快进段数向前并对齐底部   |
| `S`         | 以视口底部单位为基准，按回退/快进段数向后并对齐底部   |
| `R`         | 以视口顶部单位为基准，按回退/快进段数向前并对齐顶部   |
| `F`         | 以视口顶部单位为基准，按回退/快进段数向后并对齐顶部   |
| `A` / `D` | 向上或向下瞬间切换一个完整视口高度，不考虑文本块边界 |
| `1`         | 打开或关闭阅读菜单                               |
| `2`         | 打开或关闭树形目录侧栏                           |

文本块和块级图片都是导航单位，分隔符与空段跳过。导航段数只计文本块：图片紧邻当前锚点时可作为本次目标；已跨过文本后才遇到图片时，提前停在图片前的最后一个文本块，不跨过图片。

回退/快进段数默认为 2，并同时控制顶部和底部的回退、快进。包括菜单与目录在内的十一项快捷键均可在阅读菜单中点击键帽后重新绑定；绑定使用物理键位，可选择单个 `Ctrl`、`Alt` 或 `Shift` 附加键，重复绑定不会覆盖原设置。`A/D` 的淡入淡出默认关闭；开启后才使用浏览器的 View Transition API，关闭时直接切换而不调用该 API。所有阅读设置全局保存在 `AirnobeLibrary/user.json`。

书库顶部“设置”和阅读菜单“阅读设置”打开同一个全局面板。可调整字号、400/600 字重、段内间距、段落间距、栏宽、日文透明度和新书的三项默认显示状态；Q/E/Z 在当前书内的切换不修改全局默认。段内间距是无单位的行高倍率，默认 1.6 倍；段落间距使用 `em`，设置值 `0–2` 直接映射为 `0–2em`，默认 `1em`。排版即时预览并保持当前阅读块及视口偏移。

内置 Airnobe、Absolutely、One Dark、Xcode Dark 和暖纸浅色五套全应用主题。自定义主题是只含语义颜色的安全 JSON，保存到 `AirnobeLibrary/themes/`；格式与生成提示见 [`THEME_PROMPT.md`](./THEME_PROMPT.md)。主题不允许 CSS、URL、脚本或自定义字体。

目录从阅读菜单或默认 `2` 键打开，以左侧树形侧栏展示并高亮视口顶部所在章节。目录目标仍使用 `documentId + fragmentId`，通过虚拟列表索引跳转，不要求目标节点已经挂载。

书库、阅读菜单、目录和确认浮层支持 `W/S/A/D` 空间移动与 `Space` 确认，同时保留鼠标、Tab 和 Enter。`W/S` 在当前区域首尾循环，`A/D` 横向移动但不循环；浮层打开时空间导航优先于正文快捷键。备注、数值和选择器由 Space 进入编辑，快捷键键帽由 Space 进入录制，退出后恢复空间导航。

## 本地书库

书库使用稳定的内部书籍 ID，不依赖可读文件夹名。`AirnobeLibrary/library.json` 保存标题、作者、收藏状态、备注、内容类型、注音状态及原始 EPUB 哈希；书籍内容位于 `AirnobeLibrary/books/<library-book-id>/`。

应用默认进入桌面三栏书库：左栏按“全部、想看、在看、看过、搁置、放弃”筛选，中栏列书，右栏显示封面与书籍详情。列表按 `updatedAt` 倒序排列；单击选择，双击或按 Space 使用“继续阅读”。右栏另提供“继续阅读”和“从头阅读”：前者恢复保存的顶部块并将其顶部对齐阅读区，后者忽略旧位置从书首打开，并在后续自动保存时更新进度。收藏状态与备注在详情栏修改，原始 EPUB 可直接导出。导入成功后仍停留在书库并选中新书；阅读器右键菜单可返回书库。

书库右侧可直接使用已保存的 `source.epub` 重新导入当前书籍，无需再次选择文件；重新导入沿用原子替换并保留书库 ID、收藏状态和备注。删除当前书籍必须二次确认，并同时删除阅读数据与保存的原始 EPUB；索引更新失败时恢复原书目录。

每本书保留原始 `source.epub`，用于以后原样导出。日文增强版同时保存不含重复 assets 的压缩 P0 快照；纯中文书和注音失败后直接使用 P0 的书不额外保存快照。封面复用书籍 assets 中的原始资源，无封面时不生成截图或替代封面。

每本书的 `reading-state.json` 独立保存视口顶部所在的有效文本或图片块、百分比和章节。滚动停止后自动保存；继续阅读时将该块顶部统一对齐阅读区顶部，不复刻退出时段内的像素偏移。状态损坏或目标失效时从书首开始。进度不修改书库记录的 `updatedAt`、收藏状态或排序，替换 EPUB 时重置。

完整 SHA-256 相同的 EPUB 是精确重复项，不重新转换。OPF 标识相同，或标准化后的书名与作者相同但哈希不同，仅视为疑似新版本，必须由用户选择替换、另存或取消。替换失败时保留旧书及其书库状态。

中间格式升级后，重新导入同一 EPUB 会以原书和 P0 快照重建旧格式记录，保留书库 ID、收藏状态和备注。

当前书库不提供搜索、删除或手动排序。阅读百分比与当前章节只显示在右侧详情栏。

readingOrder 决定连续阅读顺序，TOC 只负责导航；不能假设“一份文档就是一章”。

## 平台边界

当前 React 阅读界面、书籍 AST、显示状态、块导航和阅读位置语义应保持平台无关。以下能力属于平台适配层：

- EPUB 文件选择与本地存储；
- P0/P0.5 转换执行环境；
- 菜单、键盘、触摸等输入方式；
- 应用生命周期与系统权限。

未来如开发 iPhone 版，将在同一 monorepo 中建立独立应用，共享格式和阅读核心，不复用桌面输入外壳。Tauri 2 支持 iOS，但当前 Node 转换服务需要迁移，构建与签名还需要 macOS、Xcode 和 Apple 开发环境。

详细中间格式见 [`01-epub-normalizer.md`](./01-epub-normalizer.md)，阶段状态见 [`PLAN.md`](./PLAN.md)。
