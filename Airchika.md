# Airnobe：项目上下文与已确定边界

## 1. 项目定位

Airnobe 是面向中文用户的日本轻小说阅读器，不是通用 EPUB 排版器。默认只阅读中文；日文原文和程序生成注音是两个互不绑定的可选显示层。

阅读界面采用单列、连续向下滚动。切换日文显示状态时，以当前中文块为视觉锚点补偿布局变化，避免读者丢失位置。

## 2. 固定技术与字体

- 阅读器：React、TypeScript、Vite、Tauri 2。
- P0/P0.5 工具：Node.js、TypeScript、npm workspace。
- 中文正文：Sarasa Gothic SC。
- 日文正文：Sarasa Gothic J。
- UI：Sarasa UI SC。

`Font/` 当前已经包含上述 SC、J 和 UI SC 字体的多个字重。字体打包与授权清单在 P1 处理，不进入 P0 中间格式。

## 3. 为什么先做中间格式

EPUB 只是输入容器，不是阅读器内部数据模型。当前真实样本同时存在 EPUB 2/3、不同 OPF 路径、nav/NCX、题图与正文拆页、多种 ruby 写法、Kobo span、行内外字、SVG 图片包装等差异；spine、TOC 和文件名也不能互相替代。

因此第一阶段先完成：

```text
本地无 DRM EPUB
  -> P0 归一化（不可变基础书）
  -> P0.5 可选注音派生（新的完整书籍目录）
  -> P1 阅读器消费统一 AST
```

JSON 只承担序列化。正文不保存 HTML 字符串，也不重复保存 `plainText`；搜索与调试文本由共享格式包从 AST 派生，避免两份正文失步。

## 4. 双语语义

一个文本块可以有多个 `ContentVariant`：

- 日文原文：`language: "ja-JP"`、`origin: "source"`。
- 中文译文：`language: "zh-CN"`、`origin: "translation"`。
- EPUB 没有提供译者身份时，只按 DOM 顺序保存 `order`；P1 默认显示第一个中文译文。
- 没有 auto-novel 结构证据时，不根据语言猜测段落配对。纯中文、纯日文和无法分类的内容分别保留。

当前 auto-novel 适配器只认结构证据：`opacity:0.4` 的原文 `<p>`，以及同父级、紧邻、无属性、无子元素的纯文本译文 `<p>`。适配器从段落序列边界推断 `zh-jp` 或 `jp-zh`，支持多个译文，不读取 EPUB 外部文件名。

## 5. 注音语义

P0 保留 EPUB 已有 ruby，P0.5 才生成辅助注音：

- 原生与生成 ruby 分别标记 `origin: "source" | "generated"`。
- 对整个日文块建立连续句子和 AST 偏移映射后再分词，不逐文本节点丢失上下文。
- 已有 ruby 是只读保护区；空格、标点、换行、强调、链接和行内图片保持原结构。
- 复用 Kuromoji/IPADIC 和书内唯一读音。书内读音只在分词边界完全匹配时使用。
- 按词与送假名拆分 ruby，不把词强制拆成逐汉字注音。
- 未知词、人名、造语和无法安全映射的范围保持原文；这是辅助阅读结果，不宣称出版级准确率。

P0.5 不覆盖基础目录，并拒绝对已经带 `book.json.derivation` 的书再次派生。

阅读器需要支持三种最终显示结果：

| 状态 | 中文 | 日文 | 出版社 ruby | 程序 ruby |
|---|---|---|---|---|
| 纯中文 | 显示 | 隐藏 | 不显示 | 不显示 |
| 中日原始版 | 显示 | 显示 | 显示 | 隐藏 |
| 中日注音版 | 显示 | 显示 | 显示 | 显示 |

出版社 ruby 是日文原文的一部分，显示日文时始终保留。程序 ruby 是独立辅助层，可以随时关闭。第三种状态的产品目标是补足日文汉字注音；首版仍会让低置信度、人名、造语和无法安全对齐的词保持原文，避免伪造读音。

## 6. P1 首轮阅读体验

- 默认夜间主题和单列连续滚动。
- 默认只显示中文，程序注音默认关闭。
- `Q` 只显示/隐藏对应日文。
- `W` 只显示/隐藏 `origin: "generated"` 的程序注音，不影响出版社的 `origin: "source"` ruby。
- 日文隐藏时按 `W` 仍可切换注音状态；下次显示日文时生效。纯中文书中两个按键均不改变正文。
- 日文初始透明度 70%（以后可配置）。
- 切换前记录视口锚点中文块及其顶部偏移，切换后恢复同一偏移。
- 中文、日文、UI 分别加载对应字体。
- 阅读器使用 `readingOrder` 决定连续阅读顺序，使用树形 TOC 导航；不能假设“一份文档就是一章”。

## 7. 当前真实样本基线

仓库内 5 本 EPUB 仅用于本地回归，不复制进公开 fixture：

- 4 本中日混排书：合计 15,056 个 auto-novel 配对块、5,707 个原生 ruby。
- `zjws.epub`：纯中文；nav 位于 spine，但不进入输出 readingOrder；正文保留 9,818 个文本块。
- 输入 mimetype 的顺序或压缩方式不规范只产生报告警告，不阻止可安全解析的内容。

详细格式、CLI 和验收见 [`01-epub-normalizer.md`](./01-epub-normalizer.md)，阶段路线见 [`PLAN.md`](./PLAN.md)。
