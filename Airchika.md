# Airnobe：产品与阅读行为契约

## 产品定位

Airnobe 是面向中文用户的日本轻小说阅读器，不是通用 EPUB 排版器。EPUB 只作为输入，阅读器消费统一的类型化 AST。

当前正式支持 Windows 桌面端，并已建立 Tauri 2 桌面开发壳：原生多选和窗口文件拖放均进入同一 EPUB 导入队列。P2b 完成自包含 Node sidecar 前，桌面壳仍依赖开发期本地服务，不作为可分发安装版。Android、移动网页和触摸交互不在当前范围内。

## 内容与注音

一个文本块可以同时包含中文译文和日文原文。没有可靠结构证据时不猜测配对，纯中文、纯日文和未分类内容分别保留。

P0 保留出版社 ruby，P0.5 可选地生成辅助振假名和片假名罗马音：

- `origin: "source"`：出版社注音，属于日文原文。
- `origin: "reused"`：复用书内唯一的出版社读音。
- `origin: "generated"`：由词典和分词器生成的读音。
- `readingType: "kana"`：出版社或程序振假名。
- `readingType: "romaji"`：仅对片假名词生成的改良赫本式罗马音，长音使用长音符。
- 低置信度、人名、造语和无法安全对齐的词保持原文。

出版社原生注音始终显示；辅助振假名关闭时继承日文正文颜色，开启时使用固定金色。书内复用、词典生成和片假名罗马音继承日文正文颜色与透明度。`2` 同时控制两类辅助振假名，不影响出版社注音是否显示；`3` 独立控制片假名罗马音。两项可以同时开启，日文隐藏时仍可改变其状态。

阅读器支持三种结果：

| 状态       | 中文 | 日文 | 出版社注音 | 程序振假名 |
| ---------- | ---- | ---- | ---------- | ---------- |
| 纯中文     | 显示 | 隐藏 | 隐藏       | 隐藏       |
| 中日原始版 | 显示 | 显示 | 显示       | 隐藏       |
| 中日注音版 | 显示 | 显示 | 显示       | 显示       |

片假名罗马音只表示日语读音，例如 `コンピューター → konpyūtā`，不尝试恢复 `computer` 等原始外语拼写。

## 桌面阅读行为

- 默认 Airnobe 夜间主题、单列连续滚动。日文、辅助注音和片假名罗马音是全局显示状态，切换后对后续打开的书继续生效。
- 长书使用虚拟列表，只挂载视口附近的块。
- 正文排版区分段内间距、普通段落间距和 EPUB 明确空段形成的大段分隔。大段落总间距为一个当前正文行高加两倍段落间距；空段不成为导航或进度锚点。
- 日文显示层使用非对称的双向分阶段动效：开启时先展开约 160ms、再淡入约 180ms；关闭时先完整淡出约 360ms、再收缩约 320ms。关闭期间阅读锚点保持到动画结束，快速反向切换从当前阶段继续，仅影响已挂载虚拟行；系统要求减少动态效果时瞬时切换。
- 日文与中文左边缘对齐，使用可关闭的栏外引用线区分。中文透明度默认 `1`、日文默认 `0.6`，均可在 `0.2–1` 间调整。注音字号默认是正文的 `0.6` 倍，可在 `0.45–0.8` 间调整；注音开启时先展开布局再淡入，关闭时先淡出再收紧，出版社注音始终保留且金色平滑恢复为正文色。
- 右键或默认 `Q` 键打开左右并列的阅读侧栏；正文保持可滚动，不使用遮罩。
- 中文与 UI 使用 Sarasa Gothic SC，日文使用 Sarasa Gothic J；正文提供 400/600/800 三档真实字重。

默认快捷键：

| 按键          | 行为                                                 |
| ------------- | ---------------------------------------------------- |
| `1`         | 切换日文                                             |
| `2`         | 切换书内复用与词典生成振假名                         |
| `3`         | 切换片假名罗马音                                     |
| `W`         | 以视口底部单位为基准，按滚动段数向前并对齐底部       |
| `S`         | 以视口底部单位为基准，按滚动段数向后并对齐底部       |
| `Z`         | 以视口顶部单位为基准，按滚动段数向前并对齐顶部       |
| `X`         | 以视口顶部单位为基准，按滚动段数向后并对齐顶部       |
| `A` / `D` | 向上或向下瞬间切换一个完整视口高度，不考虑文本块边界 |
| `Q`         | 打开或关闭统一阅读侧栏                               |
| `F`         | 切换全屏                                             |
| `E`         | 在书库与最后阅读界面之间切换                         |
| `C`         | 为视口顶部有效文本或图片块添加书签                   |
| `F11`       | 在 Windows 桌面版切换原生全屏                        |
| `Alt+Enter` | 切换全屏；浏览器开发版使用 Fullscreen API            |

文本块和块级图片都是导航单位，分隔符与空段跳过。导航段数只计文本块：图片紧邻当前锚点时可作为本次目标；已跨过文本后才遇到图片时，提前停在图片前的最后一个文本块，不跨过图片。

滚动段数默认为 2、范围为 1–10，并同时控制顶部和底部的回退、快进。十三项阅读快捷键可在“阅读导航／显示与应用”双栏窗口重新绑定；`F11` 与 `Alt+Enter` 继续作为固定全屏入口。绑定使用物理键位，可选择单个 `Ctrl`、`Alt` 或 `Shift` 附加键；录制时按 Backspace 可清空绑定，设置已占用组合键时交换两个动作的绑定。`A/D` 的淡入淡出默认关闭；开启后才使用浏览器的 View Transition API。所有阅读设置全局保存在 `AirnobeLibrary/user.json`。

书库顶部“设置”管理全应用主题，以及“全屏”“切换书库/阅读界面”两个全局快捷键；阅读侧栏管理主题和排版与样式，完整快捷键列表在独立窗口设置。两处修改的是同一组全局绑定，配置的全屏快捷键在书库与阅读界面均可使用。`1/2/3` 会立即保存全局显示状态。段内间距是无单位的行高倍率，默认 1.6 倍；段落间距使用 `em`，设置值 `0–2` 直接映射为 `0–2em`，默认 `1em`。正文滚动条永久隐藏，但滚轮、触控板和连续滚动保持可用；全屏时阅读侧栏滚动条也隐藏。排版即时预览并保持当前阅读块及视口偏移。

内置 Airnobe、Absolutely、One Dark、Xcode Dark、Calibre 日间、Calibre 夜间和暖纸浅色七套全应用主题。主题模式分为日间、夜间和跟随系统，并分别记住最近选择的亮色与暗色主题。自定义主题 v4 只配置 `background`、`surface`、`sidebar`、`text` 和 `accent` 五种核心颜色；弱化文字、边框、选中背景、强调色文字和危险色均由应用统一派生。正文完全使用 `text`，链接与日文引用线完全使用 `accent`；`sidebar` 只控制阅读器左右侧栏，书库左栏使用 `surface`。v1–v3 仍可读取，但旧派生色与 ruby 颜色会被忽略并按五色规则重新生成。设置界面可复制 AI 提示词并从剪贴板导入主题，主题保存到 `AirnobeLibrary/themes/`；格式见 [`THEME_PROMPT.md`](./THEME_PROMPT.md)。

书库封面和详情信息在选中、取消及换书时使用 500ms 淡入淡出；Calibre 夜间主题的阅读背景为 `#121212`，侧边栏为 `#2d2d2d`。

目录固定在统一侧栏左侧并高亮视口顶部所在章节；书签区常驻于目录下方、进度摘要上方，标题右侧的 `+` 与默认 `C` 键都用于切换当前位置书签。书签以顶部文本摘要或图片说明识别，点击精确跳转，左侧强调色按钮直接删除。目录目标仍使用 `documentId + fragmentId`，通过虚拟列表索引跳转，不要求目标节点已经挂载。

书库支持 WASD 或方向键移动。书籍行按 `Space`、`Enter` 或右键打开继续阅读、从头开始、修改状态、导出与删除菜单；修改状态进入二级五选一菜单。阅读侧栏和全部浮层使用 WASD／方向键移动、`Space`／`Enter` 确认、`Escape` 取消；侧栏展开时 W/S/E/F/A/D 正文导航仍可用。数值输入和快捷键录制期间暂停空间导航，并分别使用 Enter 提交、Escape 恢复。

## 本地书库

书库使用稳定的内部书籍 ID，不依赖可读文件夹名。`AirnobeLibrary/library.json` 保存标题、作者、收藏状态、备注、内容类型、注音状态及原始 EPUB 哈希；`app-state.json` 独立保存最后阅读的书籍 ID，精确正文位置仍保存在每书的 `reading-state.json`。书籍内容位于 `AirnobeLibrary/books/<library-book-id>/`。

应用默认进入桌面三栏书库。标题栏的书籍图标用于返回最近一次阅读的书，应用重启后仍从该书保存的顶部块和真实视口偏移继续；同一全局快捷键在阅读界面返回书库、在书库界面返回阅读。记录最后阅读书籍属于独立的辅助写入，保存失败只提示而不会阻止正文打开；书籍打开失败同样不会锁住书库或后续打开操作。左栏焦点会立即激活筛选，但不会自动聚焦筛选结果第一本；中栏按书名、状态和最近打开显示并支持表头排序，默认让最近阅读的书在最上、未打开的书在末尾。右栏仅显示封面、标题、作者、书籍信息和阅读信息；选中、取消和换书使用完整淡入淡出，封面加载完成后独立淡入。备注数据仍保留。继续阅读恢复保存的顶部块，从头阅读忽略旧位置。

书库行操作菜单可修改收藏状态、导出原始 EPUB 或删除书籍；重新导入不再提供界面入口。删除必须二次确认，并同时删除阅读数据与保存的原始 EPUB；索引更新失败时恢复原书目录。

每本书保留原始 `source.epub`，用于以后原样导出。日文增强版同时保存不含重复 assets 的压缩 P0 快照；纯中文书和注音失败后直接使用 P0 的书不额外保存快照。封面复用书籍 assets 中的原始资源，无封面时不生成截图或替代封面。

每本书的 `reading-state.json` 独立保存视口顶部所在的有效文本或图片块、该块相对视口的真实偏移、百分比和章节。滚动停止后自动保存；继续阅读先挂载目标块并恢复真实偏移，再由同一锚点控制器等待字体、图片解码和虚拟行稳定，最终精确校正。浏览器原生滚动锚定关闭，用户主动滚动会立即释放恢复锁。状态损坏或目标失效时从书首开始。进度不修改书库记录的 `updatedAt`、收藏状态或排序，替换 EPUB 时重置。

每本书的 `bookmarks.json` 独立、原子保存书签；同一正文块只保留一个书签。书签记录 `documentId + blockId + viewportOffset`，因此可以跳到尚未挂载的虚拟行并恢复真实块内偏移。替换或重新导入 EPUB 时书签与进度一同清除，精确重复导入不会清除；直接打开开发目录不持久化书签。

文件选择器和从窗口外拖放都可一次导入多个 EPUB。书库标题栏也可从「轻小说机翻机器人」导入网络小说主页：固定使用中日、Sakura→GPT 优先、EPUB 和中文文件名，从 `n.novelia.cc` 下载后交给同一查重与转换队列；首版只使用网站已有翻译，不创建翻译任务。转换按选择顺序逐本执行并在结束时汇总，单本失败不回滚已成功项目；Escape 取消重复对话框会终止尚未处理的队列。

完整 SHA-256 相同的 EPUB 是精确重复项，会自动忽略且不重新转换。OPF 标识相同，或标准化后的书名与作者相同但哈希不同，仅视为疑似新版本，可逐本选择忽略、替换或作为新书加入，并可把操作应用到后续同类冲突；覆盖遇到多个候选时仍需选择目标。替换失败时保留旧书及其书库状态。

中间格式升级后，重新导入同一 EPUB 会以原书和 P0 快照重建旧格式记录，保留书库 ID、收藏状态和备注。

当前书库不提供搜索或手动排序。阅读百分比与当前章节只显示在右侧详情栏。

readingOrder 决定连续阅读顺序，TOC 只负责导航；不能假设“一份文档就是一章”。

## 平台边界

当前 React 阅读界面、书籍 AST、显示状态、块导航和阅读位置语义应保持平台无关。以下能力属于平台适配层：

- EPUB 文件选择与本地存储；
- P0/P0.5 转换执行环境；
- 菜单、键盘、触摸等输入方式；
- 应用生命周期与系统权限。

未来如开发 iPhone 版，将在同一 monorepo 中建立独立应用，共享格式和阅读核心，不复用桌面输入外壳。Tauri 2 支持 iOS，但当前 Node 转换服务需要迁移，构建与签名还需要 macOS、Xcode 和 Apple 开发环境。

详细中间格式见 [`01-epub-normalizer.md`](./01-epub-normalizer.md)，阶段状态见 [`PLAN.md`](./PLAN.md)。
