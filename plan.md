# Airnobe 实现路线

产品行为以 [`Airchika.md`](./Airchika.md) 为准，中间格式以 [`01-epub-normalizer.md`](./01-epub-normalizer.md) 为准。

## 已完成

### P0：EPUB 归一化

- 支持 EPUB 2/3、auto-novel 中日书和纯中文书。
- 按 spine/TOC 保留阅读结构，输出可验证、可复现的 JSON AST。

### P0.5：可选注音派生

- 使用整段上下文生成词级 ruby，保护出版社原有 ruby。
- 对纯片假名分词生成改良赫本式罗马音，长音使用长音符，并由阅读器 `Z` 独立切换。
- 阅读目录只保存最终版本；日文增强版额外保存压缩的不可变 P0 快照，assets 不重复。

### P1a：最小桌面阅读器

- 直接选择 EPUB，原书与转换结果集中保存到 `AirnobeLibrary/books/<library-book-id>/`，由 `library.json` 建立索引并按完整哈希查重。
- 连续渲染 readingOrder、图片、双语内容、振假名及片假名罗马音。
- 使用虚拟滚动、阅读锚点、桌面右键菜单和可配置键盘导航。
- `W/S/R/F` 支持顶部/底部双锚点的段落导航；文本与图片均可停靠，图片不会被多段跳转跨过。
- 回退、快进段数与 `A/D` 翻页淡入淡出作为全局设置保存到 `AirnobeLibrary/user.json`；淡入淡出默认关闭，关闭时瞬间切页。
- 当前只支持桌面端，不维护移动端交互或验收。

### P1b：最小三栏书库

- 启动进入三栏书库，可按收藏状态筛选并查看数量。
- 支持选择、双击或按钮阅读、导出原始 EPUB、修改收藏状态与备注。
- 导入后停留书库并选中新书；列表按最近更新倒序排列。
- 封面直接读取书籍 asset，无封面时留空。

### P1c：阅读导航

- 左侧树形 TOC 支持虚拟列表章节跳转和当前章节高亮，默认快捷键为 `2`；默认 `1` 打开阅读菜单。
- 每本书独立保存阅读块、视口偏移、百分比和章节，打开时自动恢复。
- 书库右侧详情显示进度摘要，不影响最近更新排序和收藏状态。
- 书库、阅读菜单、目录和确认浮层支持 `W/S/A/D` 空间导航与 `Space` 确认，鼠标和原生键盘操作继续可用。

运行：`npm run reader`

## 下一步

### P1d：阅读设置

- 字号、字重、行距、栏宽、日文透明度和主题。
- 默认日文、注音与片假名罗马音显示状态。

### P2：Tauri 2 桌面封装

- 原生文件选择、本地书籍读取和转换任务。
- 展示转换状态、报告及错误。

### P3：完整书库与设置

- 搜索、删除、手动排序及书库维护。
- 注音策略和其他高级设置。

## 以后再做

iPhone 版可在同一 monorepo 中作为独立应用开发，共享格式和阅读核心。Android 不在当前路线内。

转换报告与缺失资源详情保持低优先级，继续使用现有警告和错误提示。
