# Airnobe 实现路线

产品行为以 [`Airchika.md`](./Airchika.md) 为准，中间格式以 [`01-epub-normalizer.md`](./01-epub-normalizer.md) 为准。

## 已完成

### P0：EPUB 归一化

- 支持 EPUB 2/3、auto-novel 中日书和纯中文书。
- 按 spine/TOC 保留阅读结构，输出可验证、可复现的 JSON AST。

### P0.5：可选注音派生

- 使用整段上下文生成词级 ruby，保护出版社原有 ruby。
- 对纯片假名分词生成改良赫本式罗马音，长音使用长音符，并由阅读器 `Z` 独立切换。
- 阅读目录只保存最终版本；日文增强版额外保存压缩的不可变 P0 快照，assets 不重复。

### P1a：最小桌面阅读器

- 直接选择 EPUB，原书与转换结果集中保存到 `AirnobeLibrary/books/<library-book-id>/`，由 `library.json` 建立索引并按完整哈希查重。
- 连续渲染 readingOrder、图片、双语内容、振假名及片假名罗马音。
- 使用虚拟滚动、阅读锚点、桌面右键菜单和可配置键盘导航。
- `W/S/R/F` 支持顶部/底部双锚点的段落导航；文本与图片均可停靠，图片不会被多段跳转跨过。
- 回退、快进段数与 `A/D` 翻页淡入淡出作为全局设置保存到 `AirnobeLibrary/user.json`；淡入淡出默认关闭，关闭时瞬间切页。
- 当前只支持桌面端，不维护移动端交互或验收。

### P1b：最小三栏书库

- 启动进入三栏书库，可按收藏状态筛选并查看数量。
- 支持选择、双击或按钮阅读、导出原始 EPUB、修改收藏状态与备注。
- 导入后停留书库并选中新书；列表按最近更新倒序排列。
- 封面直接读取书籍 asset，无封面时留空。

### P1c：阅读导航

- 左侧树形 TOC 支持虚拟列表章节跳转和当前章节高亮，并合入默认 `1` 打开的统一阅读侧栏；默认 `2` 返回书库。
- 每本书独立保存视口顶部阅读块、百分比和章节，继续阅读时将该块顶部对齐阅读区。
- 书库右侧详情显示进度摘要，不影响最近更新排序和收藏状态。
- 书库支持 WASD／方向键与 `Space`；阅读侧栏支持方向键与 `Space`，鼠标和原生键盘操作继续可用。

### P1d：全局阅读设置与主题（完成）

- 设置面板支持字号、400/600 字重、行距、栏宽、日文透明度及三项新书默认显示状态。
- 五套内置主题覆盖书库与阅读界面；安全主题 JSON 可导入并原子保存。
- 排版即时预览、保存失败回滚，并保持虚拟列表阅读锚点。

### P1e：三级正文间距（完成）

- 书籍格式 v4 保留 EPUB 明确空段，并折叠连续空段、清理文档首尾空段及重定向锚点。
- 阅读设置 v6 将段内间距与段落间距分开；大段落总间距为一个当前正文行高加两倍段落间距。

### P1f：扁平化书库与阅读设置（完成）

- 书库采用书名、状态、最近打开三列；状态在行内修改，`Space` 或右键打开阅读、导出和删除菜单，详情区仅显示信息。
- 选中项使用柔和主题背景，焦点使用派生的中等主题背景，不再使用外围描边。
- 阅读设置 v9 支持浅色／深色／系统主题、400/600/800 字重和范围 1–10 的回退／快进段数。
- 默认 `1/2/3` 切换日文、辅助注音和片假名罗马音，`Q` 开关侧栏，`E/F` 从顶部回退／快进；快捷键在独立窗口设置。
- 原生 ruby 保持主题金色，复用、生成和罗马音注音继承日文正文颜色。

运行：`npm run reader`

## 下一步

### P2：Tauri 2 桌面封装

- 原生文件选择、本地书籍读取和转换任务。
- 展示转换状态、报告及错误。

### P3：完整书库与设置

- 搜索、删除、手动排序及书库维护。
- 注音策略和其他高级设置。

## 以后再做

iPhone 版可在同一 monorepo 中作为独立应用开发，共享格式和阅读核心。Android 不在当前路线内。

转换报告与缺失资源详情保持低优先级，继续使用现有警告和错误提示。
