# Airnobe 实现路线

## 产品交互

默认只显示中文。日文和程序注音是两个独立选项：

- `Q`：显示/隐藏对应日文。
- `W`：显示/隐藏程序生成的注音。
- 出版社 ruby 属于日文原文；显示日文时始终保留，不受 `W` 影响。

对应三种阅读状态：纯中文、中日原始版、中日程序注音版。程序注音以可靠性优先，低置信度词允许保持原文。

## 实现原则

1. EPUB 只作为输入；阅读器只消费类型化 JSON AST。
2. readingOrder 与 TOC 独立，不假设“一文件等于一章”。
3. P0 基础书不可变，P0.5 写出新的自包含派生书。
4. ruby 用 `origin: "source" | "generated"` 区分出版社与程序注音。
5. 不静默丢内容；输出必须可验证、可复现。

## P0：EPUB 归一化

状态：首版已完成。

- 支持 EPUB2/3、auto-novel 中日书和纯中文书。
- 按 spine/TOC 保留阅读结构，把正文与图片转换为统一 AST。
- 校验临时结果后再替换正式输出。

格式与 CLI 见 [`01-epub-normalizer.md`](./01-epub-normalizer.md)。

## P0.5：可选注音派生

状态：首版已完成。

- 使用整段上下文生成词级 ruby，保护出版社原有 ruby。
- 生成节点标记为 `generated`；低置信度范围保留原文并报告。
- 不覆盖基础书，也不允许重复派生。

## P1：浏览器阅读原型

- React + TypeScript + Vite；夜间主题、单列连续滚动。
- 实现独立的 `Q` 日文开关和 `W` 程序注音开关。
- 切换日文时保持中文阅读位置；分别使用 SC/J/UI SC 字体。

暂不做书库、设置持久化和 Tauri 文件访问。

## P2：Tauri 2 桌面封装

- 导入 EPUB 或打开 Airnobe 目录；P0.5 由用户选择是否执行。
- 读取书籍与资源，展示转换报告及错误。

## P3：设置与书库

加入书库、阅读进度、排版/主题、快捷键配置及注音策略。
