# Airnobe 实现路线

产品行为以 [`Airchika.md`](./Airchika.md) 为准，中间格式以 [`01-epub-normalizer.md`](./01-epub-normalizer.md) 为准。

## 已完成

### P0：EPUB 归一化

- 支持 EPUB 2/3、auto-novel 中日书和纯中文书。
- 按 spine/TOC 保留阅读结构，输出可验证、可复现的 JSON AST。

### P0.5：可选注音派生

- 使用整段上下文生成词级 ruby，保护出版社原有 ruby。
- 对纯片假名分词生成改良赫本式罗马音，长音使用长音符，并由阅读器 `Z` 独立切换。
- 阅读目录只保存最终版本；日文增强版额外保存压缩的不可变 P0 快照，assets 不重复。

### P1a：最小桌面阅读器

- 直接选择 EPUB，原书与转换结果集中保存到 `AirnobeLibrary/books/<library-book-id>/`，由 `library.json` 建立索引并按完整哈希查重。
- 连续渲染 readingOrder、图片、双语内容、振假名及片假名罗马音。
- 使用虚拟滚动、阅读锚点、桌面右键菜单和可配置键盘导航。
- `W/S/R/F` 支持顶部/底部双锚点的段落导航；文本与图片均可停靠，图片不会被多段跳转跨过。
- 回退、快进段数与 `A/D` 翻页淡入淡出作为全局设置保存到 `AirnobeLibrary/user.json`；淡入淡出默认关闭，关闭时瞬间切页。
- 当前只支持桌面端，不维护移动端交互或验收。

### P1b：最小三栏书库

- 启动进入三栏书库，可按收藏状态筛选并查看数量。
- 支持选择、双击或按钮阅读、导出原始 EPUB、修改收藏状态与备注。
- 导入后停留书库并选中新书；列表按最近更新倒序排列。
- 封面直接读取书籍 asset，无封面时留空。

### P1c：阅读导航

- 左侧树形 TOC 支持虚拟列表章节跳转和当前章节高亮，并合入默认 `1` 打开的统一阅读侧栏；默认 `2` 返回书库。
- 每本书独立保存视口顶部阅读块、百分比和章节，继续阅读时将该块顶部对齐阅读区。
- 书库右侧详情显示进度摘要，不影响最近更新排序和收藏状态。
- 书库支持 WASD／方向键与 `Space`；阅读侧栏支持方向键与 `Space`，鼠标和原生键盘操作继续可用。

### P1d：全局阅读设置与主题（完成）

- 设置面板支持字号、400/600 字重、行距、栏宽、日文透明度及三项新书默认显示状态。
- 五套内置主题覆盖书库与阅读界面；安全主题 JSON 可导入并原子保存。
- 排版即时预览、保存失败回滚，并保持虚拟列表阅读锚点。

### P1e：三级正文间距（完成）

- 书籍格式 v4 保留 EPUB 明确空段，并折叠连续空段、清理文档首尾空段及重定向锚点。
- 阅读设置 v6 将段内间距与段落间距分开；大段落总间距为一个当前正文行高加两倍段落间距。

### P1f：扁平化书库与阅读设置（完成）

- 书库采用书名、状态、最近打开三列；状态在行内修改，`Space` 或右键打开阅读、导出和删除菜单，详情区仅显示信息。
- 选中项使用柔和主题背景，焦点使用派生的中等主题背景，不再使用外围描边。
- 阅读设置 v9 支持浅色／深色／系统主题、400/600/800 字重和范围 1–10 的回退／快进段数。
- 默认 `1/2/3` 切换日文、辅助注音和片假名罗马音，`Q` 开关侧栏，`E/F` 从顶部回退／快进；快捷键在独立窗口设置。
- 原生 ruby 保持主题金色，复用、生成和罗马音注音继承日文正文颜色。

### P1g：阅读动效、主题导入与批量书库操作（完成）

- 日文显示层使用短暂淡入淡出和高度过渡，并在虚拟列表重测期间保持顶部阅读锚点。
- 主题格式升级到精简 v2，同时兼容 v1；主题提示词可直接复制，并从剪贴板导入生成结果。
- 修复主题二维键盘导航、自绘滑轨、按钮 hover、书库字段对齐和全浮层 Space／Enter／Escape 操作。
- 文件选择与窗口拖放均支持多个 EPUB，按顺序导入并处理重复策略、部分失败和最终汇总。

### P1h：阅读显示、全屏与注音切换（完成）

- 阅读设置 v10 增加中文透明度和正文滚动条开关，并将导航设置统一称为“滚动段数”。
- Windows 桌面版支持按钮、`F11` 和 `Alt+Enter` 原生全屏；浏览器开发版兼容 Fullscreen API。
- 修复配对日文引用线和进度摘要布局；辅助振假名与片假名罗马音关闭后不再撑开汉字，切换时使用保持阅读锚点的分阶段动效。

### P1i：阅读定位、柔和动效与 Calibre 主题（完成）

- 阅读设置 v11 永久隐藏正文滚动条，保留连续滚动；继续阅读保存真实块内偏移，并在字体、图片和虚拟行稳定前锁定锚点。
- 日文、辅助注音、片假名罗马音及书库详情使用柔和渐变，出版社原生注音只在辅助注音开启时使用固定金色。

### P1h.1：双向显示动效与单一阅读锚点（完成）

- 日文和两类注音改为严格的展开、淡入、淡出、收缩阶段，快速重复切换可从当前阶段反向。
- 书库详情用真实关键帧覆盖选中、取消和换书，封面加载后独立淡入。
- 关闭浏览器原生滚动锚定；虚拟列表只补偿锚点上方已测量行，继续阅读、字体、图片和排版共用同一锚点控制器。
- 主题格式升级到 v3 并兼容 v1/v2，移除 ruby 颜色 token，新增 Calibre 日间／夜间主题。

### P1h.2：阅读动效与 Calibre 夜间主题微调（完成）

- 书库详情与封面加载的进入、退出及交叉淡化统一为 500ms。
- 日文开启保持展开 160ms、淡入 180ms；关闭改为淡出 360ms、收缩 320ms，并将单一阅读锚点锁延长到完整关闭过程之后。辅助振假名和片假名罗马音时序不变。
- Calibre 夜间主题使用 `#121212` 阅读背景和 `#2d2d2d` 侧边栏；引用线最终采用纯日文上下 `0.25em`、配对日文顶部 `0.9em` 和底部 `0.3em`。

### P1h.3：五色主题格式（完成）

- 主题格式升级到 v4，只保留背景、表面、阅读侧栏、文字和强调五种核心颜色；v1–v3 读取后统一规范化。
- 弱化文字、边框、选中背景、强调色文字和危险色由共享颜色工具派生；正文复用文字色，链接和日文引用线复用强调色。
- 书库左栏使用表面色，只有阅读器左右侧栏使用独立侧栏色；七套内置主题和剪贴板导入提示词均输出 v4。

### P1j：窗口操作、书签与引用线还原（完成）

- 阅读设置升级到 v12，新增可配置的全屏、界面切换和添加书签，默认 `Z/X/C`；书库设置同时提供全屏和界面切换绑定，且与阅读快捷键窗口共用同一设置。`Z` 在书库和阅读界面均可全屏，`X` 在阅读界面返回书库、在书库界面返回最近阅读位置。固定 `F11` 与 `Alt+Enter` 全屏入口继续保留。
- 最后阅读书籍改由独立 `app-state.json` 原子保存，不再在打开正文前强制写入阅读进度；辅助状态保存和书籍解析错误均不得锁住书库或阻断后续打开。
- 每本书使用独立 `bookmarks.json` 原子保存书签，同块再次添加时改为取消；侧栏书签区常驻并提供 `+` 按钮、摘要、章节、进度、跳转和直接删除。
- 书签跳转复用虚拟列表与真实块内偏移，翻页淡入淡出开关决定瞬时切换是否带过渡；替换 EPUB 时清除旧书签。
- 纯日文引用线使用上下各 `0.25em`；配对日文使用顶部 `0.9em`、底部 `0.3em`，并继续挂在外层折叠容器以避免裁切。

### P1k：快捷键删除、交换与双栏整理（完成）

- 阅读设置升级到 v13，快捷键允许为空；录制时 Backspace 清空绑定，已占用组合键改为交换两个动作的绑定。
- 快捷键窗口拆为“阅读导航／显示与应用”两栏，空绑定保留可点击的空键帽，两栏分别循环并支持横向切换。
- 默认绑定重置为 `1/2/3`、顶部 `Z/X`、底部 `W/S`、翻页 `A/D`、侧栏 `Q`、全屏 `F`、书签 `C`、界面切换 `E`；旧版升级保留非快捷键设置。

### P1l：轻小说机翻机器人导入（完成）

- 书库标题栏可输入 `n.novelia.cc/novel/<provider>/<novel>` 小说主页，从网站下载中日 EPUB 并进入既有查重、P0/P0.5 和批量反馈流程。
- 下载固定使用 Sakura→GPT 优先、中文文件名，不创建新的翻译任务；服务端限制 URL、同域重定向、超时和 512 MB 大小。
- 浏览器开发版在新标签打开网站，Tauri 通过仅允许 `https://n.novelia.cc/*` 的 opener 权限打开系统浏览器。

运行：`npm run reader`

## 下一步

### P2：Tauri 2 桌面封装（进行中）

- P2a 已建立 Windows 桌面开发壳、单实例窗口、原生 EPUB 多选和 Tauri 文件拖放，继续复用现有 Reader 与开发期 Node 服务。
- P2b 将现有转换与书库服务封装为自包含 Node sidecar，并切换正式应用数据目录。
- P2c 生成 Windows 安装包，验收全新安装、升级和卸载。

### P3：完整书库与设置

- 搜索、删除、手动排序及书库维护。
- 注音策略和其他高级设置。

## 以后再做

iPhone 版可在同一 monorepo 中作为独立应用开发，共享格式和阅读核心。Android 不在当前路线内。

转换报告与缺失资源详情保持低优先级，继续使用现有警告和错误提示。
